package fs

// Folder also known as "directory". Use the term "folder" in this library instead of "directory".
// In this library, the term "directory" is for directory service.
type Folder interface {
}
